ROLE: Encyclopedic writer and editor.

CONTEXT:
- Base language: English
- Planet meta (use ONLY this data for meanings/correspondences; do not invent new ones):
  - key={key}                         # e.g., "sun"
  - name_eng={name_eng}               # e.g., "Sun"
  - glyph={glyph}                     # e.g., ☉
  - emoji={emoji}                     # optional
  - type={type}                       # luminary|inner|social|outer|centaur
  - domiciles={domiciles}             # array of sign keys (e.g., ["leo"])
  - detriments={detriments}           # array of sign keys
  - exaltation={exaltation}           # sign key or None (if None/null, write "—" instead)
  - fall={fall}                       # sign key or None (if None/null, write "—" instead)  
  - day={day}                         # sunday|monday|...|saturday|None (if None/null, write "—" instead)
  - domains_keywords={domains_keywords}         # 4–7 items
  - strengths_keywords={strengths_keywords}     # 3–5 items
  - challenges_keywords={challenges_keywords}   # 3–5 items
  - love_keywords={love_keywords}               # 3–5 items
  - career_keywords={career_keywords}           # 3–5 items
  - transit_notes={transit_notes}               # 2–4 items
  - correspondences={correspondences}           # object with colors, stones, day
- Important: Replace any null/None values with "—" symbol in the output
- For correspondences, extract colors and stones as comma-separated lists from the object
- Style: neutral, concise, third-person; educational (Wikipedia-like).
- Output: CommonMark Markdown only (no HTML, no Telegram escaping).
- Use REAL line breaks (never write "\n" or "nn").
- Lists must use the bullet "• " (U+2022) at the line start.
- No numbered or "#" headings. Keep it ~1200–1800 characters.

INSTRUCTIONS:
- Write an encyclopedic entry about {name_eng} {emoji} ({glyph}), a {type} planet.
- Use EXACT section titles (bold, each on its own line). Keep a blank line between sections.
  **Overview**
  **Dignities**
  **Domains & Keywords**
  **Strengths**
  **Challenges**
  **Love & Relationships**
  **Career & Growth**
  **Transits & Timing**
  **Correspondences**
  **Historical Notes**
  **Tip of the day**
- Section content:
  - **Overview** — 2–3 sentences summarizing the planet’s role using domains_keywords.
  - **Dignities** — one line with bold labels (use "—" for null/None values):
    **Domicile:** {domiciles} · **Detriment:** {detriments} · **Exaltation:** {exaltation} (or "—" if None/null) · **Fall:** {fall} (or "—" if None/null) · **Day:** {day} (or "—" if None/null)
  - **Domains & Keywords** — 4–6 bullets from domains_keywords.
  - **Strengths** — 3–5 bullets from strengths_keywords.
  - **Challenges** — 3–5 bullets from challenges_keywords (frame as growth).
  - **Love & Relationships** — 3–5 bullets from love_keywords.
  - **Career & Growth** — 3–5 bullets from career_keywords.
  - **Transits & Timing** — 2–4 bullets from transit_notes (general effects).
  - **Correspondences** — short lines (extract from correspondences object):
    Colors: [extract "colors" array from correspondences as comma-separated list]
    Stones: [extract "stones" array from correspondences as comma-separated list]  
    Day of the week: {day} (or "—" if null/None)
  - **Historical Notes** — 2–4 cautious, general facts (e.g., traditional associations, modern use).
  - **Tip of the day** — 1–2 sentence advice/affirmation aligned with the planet.
- Avoid medical/financial/legal claims. Do not add sections beyond those listed.

OUTPUT:
Return ONLY the Markdown body (no JSON, no code fences/backticks).